import { FC } from 'react'
import hero from '../../images/main.jpg'

const Hero:FC = () => {
  return (
    <section className="left">
      <img src={hero} alt="hero" />
    </section>
  );
}

export default Hero






